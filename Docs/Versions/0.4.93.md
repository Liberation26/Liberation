# Liberation OS 0.4.93

- Restored kernel scheduler bootstrap lifecycle task creation so the first user task pipeline can run again.
- Delayed lifecycle promotion work until the timer path has produced a real 100-tick proof, so the timer proof still happens before the shell session takes over the bootstrap console.
- Updated the kernel startup trace so it now reflects that execution continues inside the scheduler rather than parking permanently in the idle path.
- Preserved the shell bootstrap flow that clears the screen first, runs LOGIN from the shell, and reads the user's credentials interactively.
