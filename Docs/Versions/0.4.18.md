# LOS 0.4.18

- fixed a memory-manager capability-check bug that could submit the same access request twice on the non-stale path
- fixed a memory-manager mailbox stall where unsupported or not-yet-negotiated requests could be left in the request queue without a reply
- memory-manager now replies with `NOT_SUPPORTED` for requests it may not handle instead of silently wedging the mailbox
