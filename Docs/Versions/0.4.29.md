# Liberation OS 0.4.29

- Added memory-manager-owned mapping records for isolated user images.
- Added staged call-block ownership inside isolated user spaces.
- Added synthetic paging-structure backing for each isolated address-space slot.
- Added a shell-side user-mode handoff wrapper that prefers the ring-3 trampoline before any fallback path.
- Kept the disk-backed isolated execution path as the preferred loader-backed route.
