# Liberation OS 0.4.48

- Removed shell-service compile-time escape warnings by escaping the displayed `\LIBERATION\...` paths correctly.
- Repaired `ShellRuntime.c` string literal corruption that had split debug `"\n"` suffixes across lines.
- Restored missing shell runtime forward declarations and completion helpers so the ring-3 launch path compiles cleanly again.
- Kept packaged build output out of the release archive.
