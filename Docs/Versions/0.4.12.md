# Liberation OS 0.4.12

- added external string library ABI in `Source/Include/Public/StringLibraryAbi.h`
- added userland string library image source under `Source/Src/Userland/Libraries/String`
- shell now uppercases authenticated command input through `\LIBERATION\LIBRARIES\STRING.ELF` before builtin or external dispatch
- added bootstrap compatibility adapter so the shell keeps the external-library ABI shape before a full library loader exists
- installed string library image at `\LIBERATION\LIBRARIES\STRING.ELF`
