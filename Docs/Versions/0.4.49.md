# Liberation OS 0.4.49

- Fixed `Scripts/BuildBoot.sh` so the shell service assembly directory is defined before the `*.S` compile loop.
- This resolves the `ShellServiceAsmDir: unbound variable` failure during the shell-service build stage.
- Marked the unused bootstrap-image dispatch helper in `ShellRuntime.c` as intentionally unused so that build output stays clean.
- Kept packaged build output out of the release archive.
