# Liberation OS 0.4.34

- Removed bootstrap fallback from shell login and string-library handling.
- Made installed disk-backed LOGIN.ELF and STRING.ELF images mandatory for normal shell operation.
- Removed bootstrap dispatch from the generic shell image-call path.
- Preserved disk-backed ELF loading as the only normal login/string execution route.
