# 0.4.75

- Removed non-status diagnostic output from the kernel and memory-manager serial paths so only status-tagged OK/FAIL lines are emitted.
- Kept the existing OK/FAIL status-screen path intact while suppressing heartbeat, attach, mapping, and other verbose trace lines that were cluttering the display.
