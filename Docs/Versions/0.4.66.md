# 0.4.66

- Fixed staged-image entry publication in the memory manager and bootstrap launch path so service task entry addresses are resolved inside the mapped ELF image before handoff.
- This addresses the post-attach invalid-opcode fault caused by launching through a low, unresolved entry value.
