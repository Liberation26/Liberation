# Liberation OS 0.4.57

- Reserved the memory-manager service's live low-half page-table frames in its memory view so AttachStagedImage can no longer claim and overwrite active paging structures during user-image staging.
