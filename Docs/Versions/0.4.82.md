# LOS 0.4.82

- Stopped the lifecycle manager from spawning endless transient diagnostic processes after the first successful ring-3 proof return.
- This removes the repeated post-proof task creation churn that kept printing `Kernel scheduler using direct-claim stack pool.` and steadily scrolling the console.
- Direct-claim stack-pool confirmation now logs only on first use instead of on every later task allocation, so a healthy post-proof boot remains quiet and readable.
