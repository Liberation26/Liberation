# 0.4.70

- Promoted scheduler-owned runtime thread stacks to prefer memory-manager-backed frame claims once the hosted memory-manager transport is fully online, context-switched, and attached.
- Kept the reserved direct-claim stack pool only as a fallback path, so the repeated `Kernel scheduler using direct-claim stack pool.` runtime line disappears once MM-backed stack claims are healthy.
- Mirrored the scheduler stack-allocation fix into both ChangedFiles and FullSource trees and bumped the package version to 0.4.70.
