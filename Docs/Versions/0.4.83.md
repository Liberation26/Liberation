# LOS 0.4.83

- Reworked kernel framebuffer log scrolling so the text console no longer shifts upward one pixel copy at a time once the screen fills.
- Quieted the default InitCommand bootstrap path: endpoint details, capability dumps, and canned shell demo traffic now stay behind the verbose flag.
- Reduced CAPSMGR bootstrap chatter to a concise import/online summary so the first ring-3 proof run stays readable and far less expensive to render.
