# Liberation OS 0.4.32

- Added memory-manager-owned user address-space descriptors.
- Added memory-manager-owned mapping-record storage and activation hooks.
- Added completion copy-back from the mapped user call block into kernel-owned shell staging.
- Added shell/runtime hooks so isolated execution prefers the memory-manager-owned address space path.
