# Liberation OS 0.4.26

- Added shell-side disk image debug tracing for image reads, ELF validation, and execution dispatch status.
- Retained the existing disk-backed image path and bootstrap fallback behavior.
- Clarified the current user-image stack handoff state in the shell runtime.
