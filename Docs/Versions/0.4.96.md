# Liberation OS 0.4.96

- Reworked the scheduler's first CPL3 proof handoff to use the existing bridge return slot directly instead of stepping down into the older chain/seal return-stack layout.
- Simplified the later proof-stage metadata so chain, contract, seal, and handoff readiness are recorded without rewriting the kernel return path underneath the live first user-task dispatch.
- This update is aimed directly at the repeated vector 6 fault after the 200-tick heartbeat, where the first user-task handoff stack was still too fragile and could jump through corrupted return-state instead of entering ring 3 cleanly.
- The full user shell bootstrap still sits after this proof path, so this archive is focused on stabilising the first live CPL3 dispatch rather than claiming the shell is already launching.
