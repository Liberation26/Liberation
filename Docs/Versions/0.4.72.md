# 0.4.72

- Fixed the 0.4.71 scheduler dispatch build break by restoring a visible declaration for `LosKernelGetBootContext()` in the patched dispatch path.
- Corrected the first-user-task trace sites to use the actual scheduler task fields `UserCodeSegmentSelector` and `UserStackSegmentSelector`, matching the shipped scheduler headers.
- Added `KernelMain.h` back into the top-level ChangedFiles and FullSource package trees so the scheduler dispatch patch no longer depends on an older header already present in the target tree.
