# Liberation OS 0.4.47

- Removed packaged build artifacts from `base/FullSource/Build` so the release contains source only.
- Added a freestanding `memcpy` implementation for the capabilities service to satisfy linker references emitted from struct copies in `CapabilitiesRegistry.c`.
