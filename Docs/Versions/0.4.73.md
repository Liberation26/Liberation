# 0.4.73

- Fixed the package-completeness regression that made `KernelMain.h` pull in a header absent from the shipped top-level source trees.
- Added `InitCommandAbi.h` and its dependency `SecureEndpointAbi.h` back into both `ChangedFiles` and `FullSource`, so the memory-manager build no longer depends on the nested `base` tree.
