- Deferred timer-driven interrupt preemption for the first live user task while it is still using the one-shot ring-3 iret handoff path.
- This prevents the interrupt-side kernel stack pointer from overwriting the prepared user-transition chain stack in `Task->ExecutionContext`, which was sending `ret` into low data bytes such as `0x0000000000001020` after timer heartbeats.
- Runtime exception diagnostics remain enabled so later faults still print the full register dump and RIP byte report.
