# LOS 0.4.80

- Moved the scheduler first-user-task handoff frame and chain stack reservations deeper into the kernel task stack so the first user-mode interrupt cannot overwrite them during privilege-stack entry.
- This targets the reset that occurred immediately after the first IRQ following the real iretq user dispatch.
