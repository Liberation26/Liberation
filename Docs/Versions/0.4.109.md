# Liberation OS 0.4.109

- Disabled interrupts on entry to the first-user-task dispatch bridge so the bridge-to-iret handoff cannot be interrupted on the synthetic handoff stack.
- Forced the scaffold task's kernel-mode bridge context to resume with IF clear; the user IF state still comes from the prepared iret frame.
- This targets the repeated `#UD` at RIP `0x0000000000001020`, which strongly suggests the synthetic bridge stack was being corrupted before the first CPL3 `iretq` completed.
