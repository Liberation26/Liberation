# Liberation OS 0.4.46

- Fixed the capabilities service initialization to match the current `LOS_CAPABILITIES_ENDPOINT_OBJECT` ABI.
- Replaced the stale `ReceiveEndpoint.Reserved0` initialization with explicit `EndpointClass` and `SecurityMode` initialization.
