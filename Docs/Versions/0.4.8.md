# Version 0.4.8

- shell now accepts command input through the shell service request path
- internal commands implemented: help, version, status, pwd, cd, echo, clear
- unknown commands now resolve as external command images under `\LIBERATION\COMMANDS`
- added `run <name>` to force external dispatch
- added weak external launch hook so the source tree has a clean execution boundary for later kernel/user ABI wiring
