# 0.4.74

- Fixed the unresolved `LosKernelGetBootContext` link by providing a weak fallback in the scheduler dispatch path, so kernel-adjacent bootstrap/link targets can build even when they do not pull in the full kernel main object.
- Silenced the current `SchedulerLifecycle.c` unused-static warnings for the page-table metadata helpers that are intentionally retained for diagnostics and follow-on launch-path work.
