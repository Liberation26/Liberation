# Liberation OS 0.4.31

- Embedded built LOGIN.ELF and STRING.ELF binaries into the shell service as installed user-image payloads.
- Implemented the shell file-read hook against the installed user-image registry.
- Removed bootstrap fallback from the normal shell image-call path.
- Made disk-backed ELF loading the authoritative path for shell command and library invocation.
