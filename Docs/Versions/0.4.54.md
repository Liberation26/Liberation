# Liberation OS 0.4.54

- Moved the memory-manager bootstrap service stack off the fixed `0x00800000` address and now place it dynamically after the staged service image with a guard gap.
- This fixes the runtime bootstrap halt where stack mapping returned status `5` (`CONFLICT`) because the service image and stack virtual ranges overlapped.
- Kept packaged build output out of the release archive.
