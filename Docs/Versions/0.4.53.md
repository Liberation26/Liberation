# Liberation OS 0.4.53

- Made `LoginMain.h` and `StringMain.h` bootstrap-only includes in `ShellRuntime.c`, so the kernel-callable shell bootstrap entry no longer fails to compile when those userland header directories are not on the include path.
- Marked `LosShellRuntimeTryDiskBackedImage` as intentionally unused for bootstrap builds, removing the shell-service warning.
- Kept packaged build output out of the release archive.


- Fixed the kernel-callable shell bootstrap build so it compiles in bootstrap mode, includes the embedded login/string sources, and links the shell user-mode entry assembly.
