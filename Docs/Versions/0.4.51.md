# Liberation OS 0.4.51

- Replaced the stray `LOS_UNUSED_PARAMETER(Operation)` call in `MemoryManagerBootstrapDispatch.c` with a freestanding-safe `(void)Operation;` so the kernel bootstrap image builds without depending on an undeclared macro.
- Kept packaged build output out of the release archive.
