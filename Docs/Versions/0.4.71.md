# 0.4.71

- Reopened the first-user-task launch path so legacy scaffold callers now promote the handoff-ready task to **LIVE** instead of marking it `complete` or closing the old non-live gate before any CPL3 dispatch happens.
- Cleared stale scaffold `complete` state during live promotion, ensuring the scheduler requeues the prepared user task for the real `iretq` bridge rather than leaving it blocked with `user-scaffold-live=0`.
- Added the scheduler dispatch, diagnostics, bridge, and header files to both ChangedFiles and FullSource package trees so the real ring-transition path is packaged consistently with the lifecycle fix.
