# LOS 0.4.9

- shell now starts in a login-required state
- prompt switches to `login>` until authentication succeeds
- `login` is external and resolves to `\LIBERATION\COMMANDS\LOGIN.ELF`
- added external login command source and build/install path
- non-login commands are rejected until login succeeds
