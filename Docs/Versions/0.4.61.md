# Liberation OS 0.4.61

- Reserved the memory-manager service's full active page-table hierarchy, not just the low-half branch, so AttachStagedImage can no longer claim and overwrite direct-map or shared high-half paging structures during user-image staging.
