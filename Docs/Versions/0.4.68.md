# 0.4.68

- Fixed hosted memory-manager request authorization so kernel bootstrap callers keep memory.allocate and memory.query authority after MM attach.
- Prevented the scheduler stack-pool reservation path from falling back to raw direct claims just because CAPSMGR is absent from the MM service image.
- Kept the post-attach frame ownership model consistent by letting kernel bootstrap traffic stay privileged until the full capability transport is available inside MM.
